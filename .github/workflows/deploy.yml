name: 'Deploy Lambdas'

on: [push]

jobs:
    lambda1:
      runs-on: ubuntu-latest
      env:
        working-directory: ./Services
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      steps:
        - uses: actions/checkout@v3
        - uses: actions/setup-node@v3
          with:
            node-version: 16
        - uses: aws-actions/configure-aws-credentials@v2
          with:
            aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
            aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            aws-region: us-east-1
        - run: zip -j lambda1.zip ./aws-projects-dotnet/lambdas/lambda1/index.mjs
        - run: aws lambda update-function-code --function-name=lambda1 --zip-file=fileb://lambda1.zip
        # - name: Setup .NET 6.x
        #   uses: actions/setup-dotnet@v3
        #   with:
        #     dotnet-version: '6.x'
        # - name: AWS CLI
        #   run: dotnet tool install -g Amazon.Lambda.Tools
        # - name: Install dependencies
        #   run: dotnet restore ./aws-projects-dotnet/lambdas/bpd-lambdas.sln
        # - name: Build
        #   run: dotnet build --no-restore ./aws-projects-dotnet/lambdas/bpd-lambdas.sln
        # - uses: aws-actions/configure-aws-credentials@v2
        #   with:
        #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        #     aws-region: us-east-1
        # - run: zip -j lambda1.zip ./aws-projects-dotnet/lambdas/Bpd.Order.Processing.Lambda/Function.cs
        # - run: aws lambda update-function-code --function-name=lambda1 --zip-file=fileb://lambda1.zip